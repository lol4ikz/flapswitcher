#ifndef F_CPU
#define F_CPU 1000000
#endif

#include <avr/io.h>
#include <avr/interrupt.h>

ISR(INT0_vect) {
    PORTB ^= (1<<PB4);
}	


int main(void) {
    MCUCR |= (1<<ISC01);
    MCUCR |= (1<<ISC00);
    GIMSK |= (1<<INT0);
    DDRB |= (1<<PB4);
    sei();
    while(1){};
}
